<!DOCTYPE html>
<html>
  <head>
    <title>EchoTerra</title>
    <link rel="stylesheet" href="nav.css">
  </head>

  <body>
    <ul id="navigation">
      <li id="nav-logo">
        <a href="index.html">
          <img src="logo.png" alt="EchoTerra logo">
        </a>
      </li>

      <li><a href="index.html" class="active">⌂ Home</a></li>
      <li><a href="about.html">✧ About Us</a></li>
      <li><a href="product.html">✦ Products & Services</a></li>
      <li><a href="tips.html">✾ Eco Tips</a></li>
      <li><a href="EcoScanner.html">◎ Eco Scanner</a></li>
      <li><a href="calculator.html">∑ Calculator</a></li>
      <li><a href="faq.html">？ FAQ</a></li>
      <li><a href="register.html">✎ Register</a></li>
      <li><a href="bag.html">✶ Shopping Bag</a></li>
      <li><a href="contact.html">✉ Contact Us</a></li>
    </ul>

<!--- Make sure that you add class="active" on the page that you are working on. 
For example:  <li><a href="index.html" class="active">⌂ Home</a></li>   -->

    <!-- rest of your page content goes here -->
  </body>
</html>


CSS
/* The comments we can include only on Home page, so if you are doing another page, just delete the comments*/

body { 
  /* We set the general look for the whole page: background, font and text color. */
  background-color: #fbf7f2;
  font-family: Georgia, "Goudy Old Style", "Times New Roman", serif;
  color: #1f3d2b;
  margin: 20px;
}

/* This list works as our navigation bar and we give it a pill shape. */
#navigation {
  list-style-type: none;     /* remove default bullets */
  margin: 10px auto;
  padding: 6px;
  width: fit-content;        /* bar becomes only as wide as its content */
  background-color: #faf5ef;
  border-radius: 999px;      /* very large radius = rounded bar */
  border: 1px solid #8a9b5a;
  overflow: hidden;          /* keeps children inside the rounded edges */
}

/* We put every menu item next to each other horizontally. */
#navigation li {
  float: left;
}

/* This is the place where we keep the logo on the left side of the bar. */
#nav-logo {
  padding: 0 10px;
}

/* The logo behaves like a simple image link with no button styling. */
#nav-logo a {
  display: block;
  padding: 0;
  background: none;
  border-radius: 0;
}

#nav-logo a:hover,
#nav-logo a:active {
  background: none;
  color: inherit;            /* logo does not change color on hover/click */
}

/* Size and shape of the logo image. */
#nav-logo img {
  height: 34px;
  width: 34px;
  border-radius: 0;
  display: block;
}

/* Here we style all the normal text links in the menu (not the logo). */
#navigation li:not(#nav-logo) > a {
  display: block;
  padding: 10px 20px;        /* controls button size */
  text-align: center;
  text-decoration: none;
  font-size: 1rem;
  border-radius: 18px;       /* gives each link rounded corners */
  background-color: transparent;
}

/* Default text color for links, whether visited or not. */
#navigation li:not(#nav-logo) > a:link,
#navigation li:not(#nav-logo) > a:visited {
  color: #1f3d2b;
}

/* Visual feedback when the mouse is over a menu item. */
#navigation li:not(#nav-logo) > a:hover {
  background-color: #7E9DA2;
  color: #fbf7f2;
}

/* Color while the user is clicking on a menu item. */
#navigation li:not(#nav-logo) > a:active {
  color: #fbf7f2;
}

/* This style marks the link of the page we are currently on. */
#navigation li:not(#nav-logo) > a.active {
  background-color: #dfe8dd;
  color: #1f3d2b;
}

/* We clear the floats so that content under the menu starts correctly. */
#navigation::after {
  content: "";
  display: block;
  clear: both;
}







